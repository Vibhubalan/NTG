import type { RankedReview } from "@/lib/review-utils";

/**
 * Optional extra Google reviews pasted from your Maps listing.
 * Google Places API only returns up to 5 reviews per day — add more here
 * (no API cost; used only when you need a longer carousel).
 *
 * Paste real reviews from: Google Maps → NTG Lounge → Reviews
 */
export const supplementalReviews: RankedReview[] = [];
